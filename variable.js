// In JavaScript, a variable is a named place to store a value so you can use or change it later.
let name = "Vishal";
let age = 20;
const country = "India";

console.log(name);
console.log(age);

/*
JavaScript has three ways to declare variables:
let — value can be changed.
const — value cannot be reassigned.
var — older way; generally avoid it in modern JavaScript.

 */

let score = 10;
score = 20;       // allowed

const pi = 3.14;
// pi = 4;        // Error
